import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  margin-left: 250px;
  margin-top: 5px;
`;

export default () => (
  <Container>
    <p>Tweets NAV</p>
  </Container>
);
